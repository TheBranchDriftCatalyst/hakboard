# TODO: create this file at the end for seamless dev setup and deployment
# for now i am going to just dump all command notes here

# make dev_setup
# install nix if not installed
# sh <(curl -L https://nixos.org/nix/install)
# yarn install

